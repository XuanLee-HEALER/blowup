{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for blowup desktop",
  "windows": ["main", "player"],
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:default"
  ]
}
